This is my final project for my User Management API program for the course I'm taking. Copilot was used to assist, debug and suggest middleware that could be used. Each stage of the project has been branched off for easier comparing depending what was required at the time of that project part. 

This is a minialist API, so there is no controller.cs file, everything is contained into one place for ease as it's a small project (anything bigger would have dedicated .cs files.)
